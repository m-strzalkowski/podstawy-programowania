#include <stdio.h>
#include <stdlib.h>
#include "drzewo.h"

/*
typedef int drzdane;
typedef struct drzelem
{
    drzdane wart;//wartosc
    drzelem * rodzic;
    drzelem * lewy;
    drzelem * prawy;
};
*/

void dodajdodrzewa(drzelem ** drzewo, ktorypotomek kt)
{
    drzelem * nowy = (drzelem *)malloc(sizeof(drzelem));
}
